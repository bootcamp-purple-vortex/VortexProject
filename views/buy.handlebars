<div class="row">
  <div class="col-lg-12">
    <nav class="navbar navbar-expand-sm navbar-dark">
      <div class="container">
        <!-- Navbar brand -->
        <a class="toyoda-brand" href="/">TOYoda</a>

        <!-- Collapse button -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav"
          aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible content -->
        <div class="collapse navbar-collapse" id="basicExampleNav">
          <!-- Links -->

          <ul class="navbar-nav mr-auto form-inline float-left">

            <li class="text-center">
              <div class="text-center">
                <a class="btn btn-rounded waves-effect" id="sell-button" href="/sell">Sell
                  Your Item CATS</a>
              </div>
            </li>
          </ul>

          <!-- Links -->

          <form class="form-inline float-right">

            <ul class="right navbar-nav nav-flex-icons">

              <li class="nav-item">
                <div class="g-signin2" data-onsuccess="onSignin" style="margin-top: 2px; margin-left: 3px; ">
                </div>
              </li>
            </ul>

          </form>
        </div>
        <!-- Collapsible content -->
      </div>
    </nav>

    <div class="container" id="buy-card">
      <div class="row">
        <div class="col-md-12">
          <div class="card card-cascade narrower mt-6">

            <div class="row">
              <div class="col-md-6">
                <div class='view view-cascade overlay'>
                  <img class="image img-fluid" id='buy-image' src="{{toys.image}}" />
                </div>

              </div>
              <div class="col-md-6">
                <div class='card-body card-body-cascade'>
                  {{!-- <p class"card-text">
                    <strong>ID</strong>: {{toys.id}}</p>
                  <p> --}}

                    <h1 class="card-title" id='buy-title'>{{toys.toysname}}</h1>
                    <hr>
                    <h4 class="card-title">${{toys.price}}</h3>
                      <br>
                      <p class"card-text">
                        <strong>Location</strong>: {{toys.location}}</p>
                      <p class"card-text">
                        <strong>Description:</strong> {{toys.description}}</p>
                      <p class"card-text">
                        <strong>Seller:</strong> {{toys.username}}</p>

                    {{!-- <button type="button" class="btn btn-primary" data-target="#sellModal" id="buysubmit">Submit</button> --}}
                    <a type="button" class="btn btn-light-blue" id="buysubmit" data="{{toys.id}}">BUY NOW</a>
                    <a class="btn btn-rounded btn-success" href="/">Back Home</a>

                </div>

              </div>
            </div>

          </div>
        </div>
      </div>
    </div>

    <!-- Modal -->
            <div class="modal fade" id="buyModal" tabindex="-1" role="dialog" aria-labelledby="buyModal"
                aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="sellModalLabel">You purchased {{toys.toysname}}!</h5>
                        </div>
                        {{!-- <div class="modal-body">
                            List another item? Or return to the home page?
                        </div> --}}
                        <div class="modal-footer">
                            <a class="btn btn-rounded btn-secondary" href="/">Return Home</a>
                        </div>
                    </div>
                </div>
            </div>

  </div>
</div>

<script type="text/javascript" src="../js/buy.js"></script>